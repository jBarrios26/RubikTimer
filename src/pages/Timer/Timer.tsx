import React from 'react';
import { TimerContainer, TimerSpace, TimerUtilities } from './Timer.styles';

const Timer: React.FC = () => {
  return (
    <TimerContainer>
      <TimerSpace>Hola</TimerSpace>
      <TimerUtilities>Mundo</TimerUtilities>
    </TimerContainer>
  );
};

export default Timer;
